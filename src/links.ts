export interface Link {
  link: string;
  title: string;
}

export interface Links {
  model?: Link;
  properties?: Link;
  actions?: Link;
  things?: Link;
  subscriptions?: Link;
  type?: Link;
  product?: Link;
  help?: Link;
  ui?: Link;
}
